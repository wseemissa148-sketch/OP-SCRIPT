--[[
    Ultimate Universal Hub (Blox Fruits & PVP Combat)
    Update: Fast Attack targets All (Players+Mobs) & Aimbot Keybinds
    Key: 1w2s3e4e5m
]]

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local HttpService = game:GetService("HttpService")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

if getgenv().UniversalSpeedHubCleanUp then
    getgenv().UniversalSpeedHubCleanUp()
end

local Connections = {}
local CorrectKey = "1w2s3e4e5m"
local KeyUnlocked = false
local IsAssigningKey = false

-- ==========================================
-- 1. الإعدادات والمتغيرات (Global Config)
-- ==========================================
_G.Config = {
    SpeedEnabled = false,
    SpeedMultiplier = 2,
    MinSpeed = 1,
    MaxSpeed = 10,
    ToggleKey = "L",

    AutoFarm = false,
    FastAttack = false,
    AutoHaki = true,
    UseSkills = true,
    SelectWeapon = "Melee",
    AutoMaterial = false,
    MaterialMode = "Original Mobs",
    SelectMaterial = "Bones",
    SelectedMaterialSea = 1,
    TargetPVP = false,
    SelectedPlayer = "",
    SelectedIslandSea = 1,
    SelectIsland = "Cafe",
    IsTeleporting = false,
    FruitESP = false,
    AutoGrab = false,
    AttackDelay = 0.01,

    ComboSequence = {"Melee", "Blox Fruit", "Sword", "Gun"},
    ComboWeaponEnabled = {
        ["Melee"] = true, ["Sword"] = true, ["Blox Fruit"] = true, ["Gun"] = true
    },
    ComboSkills = {
        ["Melee"] = {Z = true, X = true, C = true, V = true, F = false},
        ["Sword"] = {Z = true, X = true, C = false, V = false, F = false},
        ["Blox Fruit"] = {Z = true, X = true, C = true, V = true, F = false},
        ["Gun"] = {Z = true, X = true, C = false, V = false, F = false}
    },

    PlayerAimbot = false,
    PlayerAimbotKey = "G",
    MobAimbot = false,
    MobAimbotKey = "H",
    TargetPart = "Head",
    HitboxSize = 18,
    ShowHealth = false,
    HealthStyle = "Modern Bar"
}

local configFile = "BloxFruitsConfig.json"

local function SaveConfig()
    pcall(function()
        if writefile then writefile(configFile, HttpService:JSONEncode(_G.Config)) end
    end)
end

local function LoadConfig()
    pcall(function()
        if readfile and isfile and isfile(configFile) then
            local decoded = HttpService:JSONDecode(readfile(configFile))
            for k, v in pairs(decoded) do
                if type(v) == "table" and type(_G.Config[k]) == "table" then
                    for k2, v2 in pairs(v) do _G.Config[k][k2] = v2 end
                else
                    _G.Config[k] = v
                end
            end
        end
    end)
end
LoadConfig()

local islandsBySea = {
    [1] = {["Starter Island"] = Vector3.new(1095, 16, 1420), ["Jungle"] = Vector3.new(-1610, 37, 148)},
    [2] = {["Cafe"] = Vector3.new(-380, 73, 300), ["Green Zone"] = Vector3.new(-2300, 70, -3000)},
    [3] = {["Port Town"] = Vector3.new(-2900, 15, 5300), ["Hydra Island"] = Vector3.new(5700, 600, 200)}
}
local materialsBySea = {
    [1] = {"Leather", "Magma Ore"}, [2] = {"Ectoplasm", "Vampire Fang"}, [3] = {"Bones", "Mini Tusk"}
}

-- ==========================================
-- 2. إعداد الـ GUI
-- ==========================================
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "MergedUniversalHub"
ScreenGui.ResetOnSpawn = false
if syn and syn.protect_gui then syn.protect_gui(ScreenGui) ScreenGui.Parent = CoreGui
elseif gethui then ScreenGui.Parent = gethui() else ScreenGui.Parent = CoreGui end

local KeyFrame = Instance.new("Frame", ScreenGui)
KeyFrame.Size = UDim2.new(0, 300, 0, 180)
KeyFrame.Position = UDim2.new(0.5, -150, 0.4, -90)
KeyFrame.BackgroundColor3 = Color3.fromRGB(20, 22, 28)
Instance.new("UICorner", KeyFrame).CornerRadius = UDim.new(0, 12)
local KeyInput = Instance.new("TextBox", KeyFrame)
KeyInput.Size = UDim2.new(0.85, 0, 0, 38)
KeyInput.Position = UDim2.new(0.075, 0, 0.28, 0)
KeyInput.Text = ""
local SubmitBtn = Instance.new("TextButton", KeyFrame)
SubmitBtn.Size = UDim2.new(0.85, 0, 0, 36)
SubmitBtn.Position = UDim2.new(0.075, 0, 0.58, 0)
SubmitBtn.Text = "تأكيد المفتاح 🔓"

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 540, 0, 380)
MainFrame.Position = UDim2.new(0.5, -270, 0.2, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 17, 23)
MainFrame.Visible = false
MainFrame.Draggable = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

local Sidebar = Instance.new("Frame", MainFrame)
Sidebar.Size = UDim2.new(0, 140, 1, 0)
Sidebar.BackgroundColor3 = Color3.fromRGB(20, 23, 32)
local SideLayout = Instance.new("UIListLayout", Sidebar)

local ContentArea = Instance.new("Frame", MainFrame)
ContentArea.Size = UDim2.new(1, -145, 1, -10)
ContentArea.Position = UDim2.new(0, 145, 0, 5)
ContentArea.BackgroundTransparency = 1

local ToggleBtn = Instance.new("TextButton", ScreenGui)
ToggleBtn.Size = UDim2.new(0, 75, 0, 30)
ToggleBtn.Position = UDim2.new(0.02, 0, 0.1, 0)
ToggleBtn.Text = "MENU"
ToggleBtn.Visible = false
ToggleBtn.MouseButton1Click:Connect(function() if KeyUnlocked then MainFrame.Visible = not MainFrame.Visible end end)

local tabs = {}
local function CreateTab(tabName)
    local TabBtn = Instance.new("TextButton", Sidebar)
    TabBtn.Size = UDim2.new(0.9, 0, 0, 28)
    TabBtn.Text = tabName
    local TabPage = Instance.new("ScrollingFrame", ContentArea)
    TabPage.Size = UDim2.new(1, 0, 1, 0)
    TabPage.BackgroundTransparency = 1
    TabPage.Visible = false
    local PageLayout = Instance.new("UIListLayout", TabPage)
    PageLayout.Padding = UDim.new(0, 6)
    TabBtn.MouseButton1Click:Connect(function()
        for _, t in pairs(tabs) do t.Page.Visible = false end
        TabPage.Visible = true
    end)
    table.insert(tabs, {Btn = TabBtn, Page = TabPage})
    return TabPage
end

local function AddToggle(parentPage, name, configVar, callback)
    local Button = Instance.new("TextButton", parentPage)
    Button.Size = UDim2.new(0.96, 0, 0, 30)
    Button.BackgroundColor3 = Color3.fromRGB(24, 27, 38)
    Button.Text = "  " .. name
    Button.TextColor3 = _G.Config[configVar] and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(200, 200, 200)
    Button.TextXAlignment = Enum.TextXAlignment.Left

    local Indicator = Instance.new("Frame", Button)
    Indicator.Size = UDim2.new(0, 26, 0, 14)
    Indicator.Position = UDim2.new(1, -32, 0.5, -7)
    Indicator.BackgroundColor3 = _G.Config[configVar] and Color3.fromRGB(0, 170, 255) or Color3.fromRGB(45, 50, 62)
    local Dot = Instance.new("Frame", Indicator)
    Dot.Size = UDim2.new(0, 8, 0, 8)
    Dot.Position = _G.Config[configVar] and UDim2.new(1, -10, 0.5, -4) or UDim2.new(0, 2, 0.5, -4)

    local function updateVisuals()
        local state = _G.Config[configVar]
        Indicator.BackgroundColor3 = state and Color3.fromRGB(0, 170, 255) or Color3.fromRGB(45, 50, 62)
        Dot.Position = state and UDim2.new(1, -10, 0.5, -4) or UDim2.new(0, 2, 0.5, -4)
        Button.TextColor3 = state and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(200, 200, 200)
    end

    Button.MouseButton1Click:Connect(function()
        _G.Config[configVar] = not _G.Config[configVar]
        SaveConfig()
        updateVisuals()
        if callback then callback(_G.Config[configVar]) end
    end)
    
    return {
        Toggle = function()
            _G.Config[configVar] = not _G.Config[configVar]
            SaveConfig()
            updateVisuals()
            if callback then callback(_G.Config[configVar]) end
        end
    }
end

local function CreateKeybind(parentPage, title, configVar)
    local Btn = Instance.new("TextButton", parentPage)
    Btn.Size = UDim2.new(0.96, 0, 0, 30)
    Btn.BackgroundColor3 = Color3.fromRGB(35, 40, 52)
    Btn.Text = title .. " [ " .. tostring(_G.Config[configVar]) .. " ]"
    Btn.TextColor3 = Color3.fromRGB(220, 220, 220)
    
    Btn.MouseButton1Click:Connect(function()
        IsAssigningKey = true
        Btn.Text = "اضغط الزر الآن..."
        Btn.TextColor3 = Color3.fromRGB(255, 200, 50)
        
        local conn
        conn = UserInputService.InputBegan:Connect(function(input, gp)
            if input.UserInputType == Enum.UserInputType.Keyboard then
                _G.Config[configVar] = input.KeyCode.Name
                SaveConfig()
                Btn.Text = title .. " [ " .. input.KeyCode.Name .. " ]"
                Btn.TextColor3 = Color3.fromRGB(220, 220, 220)
                IsAssigningKey = false
                if conn then conn:Disconnect() end
            end
        end)
        table.insert(Connections, conn)
    end)
    return Btn
end

local SpeedTab = CreateTab("🚀 Speed")
local FarmTab = CreateTab("🌾 Farm & Attack")
local CombatTab = CreateTab("🎯 Aimbot Settings")
tabs[1].Page.Visible = true

-- [Speed Tab]
local ToggleSpeedBtn = Instance.new("TextButton", SpeedTab)
ToggleSpeedBtn.Size = UDim2.new(0.96, 0, 0, 32)
ToggleSpeedBtn.BackgroundColor3 = Color3.fromRGB(230, 50, 50)
ToggleSpeedBtn.Text = "السرعة: [معطل ❌]"
CreateKeybind(SpeedTab, "زر إخفاء الـ UI:", "ToggleKey")

-- [Farm & Attack Tab]
AddToggle(FarmTab, "Fast Attack Universal (Players + Mobs)", "FastAttack")
AddToggle(FarmTab, "Auto Farm Level", "AutoFarm")

-- [Aimbot Tab]
local PlayerAimbotObj = AddToggle(CombatTab, "Player Camera Aimbot", "PlayerAimbot")
CreateKeybind(CombatTab, "زر تشغيل/إيقاف أيمبوت اللاعبين:", "PlayerAimbotKey")

local MobAimbotObj = AddToggle(CombatTab, "Mob Camera Aimbot", "MobAimbot")
CreateKeybind(CombatTab, "زر تشغيل/إيقاف أيمبوت الوحوش:", "MobAimbotKey")

-- ==========================================
-- 3. العمليات والأحداث (Functions & Events)
-- ==========================================

-- نظام Fast Attack المحدث (يضرب اللاعبين والوحوش معاً)
function FastAttackUniversal()
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local targets = {}
    
    -- استهداف الوحوش (Mobs)
    local enemies = workspace:FindFirstChild("Enemies")
    if enemies then
        for _, mob in pairs(enemies:GetChildren()) do
            local hrp = mob:FindFirstChild("HumanoidRootPart")
            local hum = mob:FindFirstChild("Humanoid")
            if hrp and hum and hum.Health > 0 and (char.HumanoidRootPart.Position - hrp.Position).Magnitude <= 70 then
                table.insert(targets, hrp)
            end
        end
    end
    
    -- استهداف اللاعبين (Players)
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer and p.Character then
            local hrp = p.Character:FindFirstChild("HumanoidRootPart")
            local hum = p.Character:FindFirstChild("Humanoid")
            if hrp and hum and hum.Health > 0 and (char.HumanoidRootPart.Position - hrp.Position).Magnitude <= 70 then
                table.insert(targets, hrp)
            end
        end
    end
    
    -- تنفيذ الهجوم
    if #targets > 0 then
        pcall(function()
            ReplicatedStorage:WaitForChild("Modules"):WaitForChild("Net"):WaitForChild("RE/RegisterAttack"):FireServer(0.001)
            for _, hitTarget in pairs(targets) do
                local attackId = tostring(math.random(100000, 999999))
                ReplicatedStorage:WaitForChild("Modules"):WaitForChild("Net"):WaitForChild("RE/RegisterHit"):FireServer(hitTarget, {}, attackId)
            end
        end)
        local equippedTool = char:FindFirstChildOfClass("Tool")
        if equippedTool then equippedTool:Activate() end
    end
end

task.spawn(function()
    while true do
        if _G.Config.FastAttack and KeyUnlocked then
            FastAttackUniversal()
        end
        task.wait(_G.Config.AttackDelay)
    end
end)

-- محرك Aimbot المحدث
local lockedTargetPart = nil
local function GetClosestToCamera(isPlayerTarget)
    local closestPart = nil
    local shortestDistance = math.huge
    local viewportCenter = Vector2.new(Camera.ViewportSize.X / 2, Camera.ViewportSize.Y / 2)

    if isPlayerTarget then
        for _, p in pairs(Players:GetPlayers()) do
            if p ~= LocalPlayer and p.Character then
                local targetPart = p.Character:FindFirstChild(_G.Config.TargetPart) or p.Character:FindFirstChild("HumanoidRootPart")
                local hum = p.Character:FindFirstChild("Humanoid")
                if targetPart and hum and hum.Health > 0 then
                    local screenPos = Camera:WorldToViewportPoint(targetPart.Position)
                    local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - viewportCenter).Magnitude
                    if screenDist < shortestDistance then
                        shortestDistance = screenDist; closestPart = targetPart
                    end
                end
            end
        end
    else
        local enemies = workspace:FindFirstChild("Enemies")
        if enemies then
            for _, mob in pairs(enemies:GetChildren()) do
                local targetPart = mob:FindFirstChild(_G.Config.TargetPart) or mob:FindFirstChild("HumanoidRootPart")
                local hum = mob:FindFirstChild("Humanoid")
                if targetPart and hum and hum.Health > 0 then
                    local screenPos = Camera:WorldToViewportPoint(targetPart.Position)
                    local screenDist = (Vector2.new(screenPos.X, screenPos.Y) - viewportCenter).Magnitude
                    if screenDist < shortestDistance then
                        shortestDistance = screenDist; closestPart = targetPart
                    end
                end
            end
        end
    end
    return closestPart
end

table.insert(Connections, RunService.RenderStepped:Connect(function()
    if not KeyUnlocked then return end
    if _G.Config.PlayerAimbot or _G.Config.MobAimbot then
        if not lockedTargetPart or not lockedTargetPart.Parent or not lockedTargetPart.Parent:FindFirstChild("Humanoid") or lockedTargetPart.Parent.Humanoid.Health <= 0 then
            lockedTargetPart = GetClosestToCamera(_G.Config.PlayerAimbot)
        end
        if lockedTargetPart then
            pcall(function() Camera.CFrame = CFrame.new(Camera.CFrame.Position, lockedTargetPart.Position) end)
        end
    else
        lockedTargetPart = nil
    end
end))

-- إدخالات الأزرار العالمية (UI Toggle + Aimbot Toggles)
table.insert(Connections, UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed or not KeyUnlocked or IsAssigningKey then return end
    
    if input.UserInputType == Enum.UserInputType.Keyboard then
        local keyName = input.KeyCode.Name
        
        if keyName == _G.Config.ToggleKey then
            MainFrame.Visible = not MainFrame.Visible
        elseif keyName == _G.Config.PlayerAimbotKey then
            if PlayerAimbotObj then PlayerAimbotObj.Toggle() end
        elseif keyName == _G.Config.MobAimbotKey then
            if MobAimbotObj then MobAimbotObj.Toggle() end
        end
    end
end))

-- زر مفتاح الدخول
SubmitBtn.MouseButton1Click:Connect(function()
    if KeyInput.Text == CorrectKey then
        KeyUnlocked = true; KeyFrame:Destroy(); MainFrame.Visible = true; ToggleBtn.Visible = true
    end
end)

ToggleSpeedBtn.MouseButton1Click:Connect(function()
    _G.Config.SpeedEnabled = not _G.Config.SpeedEnabled
    ToggleSpeedBtn.Text = _G.Config.SpeedEnabled and "السرعة: [شغال ⚡]" or "السرعة: [معطل ❌]"
    ToggleSpeedBtn.BackgroundColor3 = _G.Config.SpeedEnabled and Color3.fromRGB(0, 180, 100) or Color3.fromRGB(230, 50, 50)
end)

table.insert(Connections, RunService.Heartbeat:Connect(function(delta)
    if _G.Config.SpeedEnabled and KeyUnlocked then
        local char = LocalPlayer.Character
        if char then
            local hrp = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChild("Humanoid")
            if hrp and hum and hum.MoveDirection.Magnitude > 0 then
                hrp.CFrame = hrp.CFrame + (hum.MoveDirection * (_G.Config.SpeedMultiplier * 14) * delta)
            end
        end
    end
end))

getgenv().UniversalSpeedHubCleanUp = function()
    for _, conn in ipairs(Connections) do if conn then conn:Disconnect() end end
    if ScreenGui then ScreenGui:Destroy() end
end
